syntax = "proto3";

//namespace
package taipower;

//Ancillary Service Input Points
service AncillaryInputs {
    rpc UpdatePointValues (PointValues) returns (Reply) {

    }
}

// Inputs
message PointValues {
    string values = 1;
}

// Outputs
message Reply {
    bool success = 1;
}